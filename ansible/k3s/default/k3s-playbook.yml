---
- hosts: localhost
  connection: local
  gather_facts: false
  vars_files: [vars.yaml]
  vars:
    # terraform integration (optional).
    terraform_workspace: "{{ lookup('env', 'TF_WORKSPACE') | default('', true) }}"
    terraform_module_path: "{{ lookup('env', 'TERRAFORM_NODE_SOURCE') | default('tofu/aws/modules/cluster_nodes') }}"
    terraform_state_path: "{{ 'terraform.tfstate.d/' ~ terraform_workspace ~ '/terraform.tfstate' if terraform_workspace != '' and terraform_workspace != 'default' else 'terraform.tfstate' }}"
    terraform_state_file: "{{ playbook_dir | dirname | dirname | dirname }}/{{ terraform_module_path }}/{{ terraform_state_path }}"
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null"
  tasks:
    - name: Set variables based on tofu or environment
      when: kube_api_host is not defined or fqdn is not defined
      block:
      - name: Check if using tfstate file
        ansible.builtin.stat:
          path: "{{ terraform_state_file }}"
        register: tfstate_file_stat

      - name: Set fqdn and kube_api_host content from tfstate file if it exists
        ansible.builtin.set_fact:
          kube_api_host: "{{ lookup('cloud.terraform.tf_output', 'kube_api_host', state_file=terraform_state_file) }}"
          fqdn: "{{ lookup('cloud.terraform.tf_output', 'fqdn', state_file=terraform_state_file) }}"
          use_terraform: true
        when: tfstate_file_stat.stat.exists

      - name: Set kube_api_host content from env or vars when not using terraform
        ansible.builtin.set_fact:
          kube_api_host: "{{ lookup('env', 'KUBE_API_HOST') | default(kube_api_host if kube_api_host is defined else '', true) }}"
          use_terraform: false
        when: not tfstate_file_stat.stat.exists

      - name: Set fqdn content from env when not using terraform
        ansible.builtin.set_fact:
          fqdn: "{{ lookup('env', 'FQDN') }}"
        when:
          - not tfstate_file_stat.stat.exists
          - lookup('env', 'FQDN') | length > 0

    - name: Validate required variables
      assert:
        that:
          - kubernetes_version is defined
          - kubernetes_version | length > 0
          - kube_api_host is defined
          - kube_api_host | length > 0
        fail_msg: "Required variables not defined. Please ensure kubernetes_version and kube_api_host are set in vars.yaml or environment variables"

- hosts: all
  vars:
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null"
  become: true
  gather_facts: false
  vars_files: [vars.yaml]
  pre_tasks:
    - name: Wait for SSH to become available
      ansible.builtin.wait_for_connection:
        delay: 10
        timeout: 300
      become: false
    - name: Validate required variables
      assert:
        that:
          - kubernetes_version is defined
          - kubernetes_version | length > 0
          - kube_api_host is defined
          - kube_api_host | length > 0
        fail_msg: "Required variables not defined. Set kubernetes_version and kube_api_host in vars.yaml or under all.vars on the inventory file. kube_api_host should point to a control-plane node."
      run_once: true
    - include_role:
        name: setup
    - name: Gather facts
      setup:
  tasks: []

- hosts: master
  become: true
  gather_facts: false
  vars_files: [vars.yaml]
  vars:
    node_token_file: "/tmp/node_token.txt"
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null"
  pre_tasks:
    - include_role:
        name: setup
    - name: Gather facts
      setup:

  tasks:
    - name: Create K3s server manifests directory on master
      file:
        path: "{{ item }}"
        state: directory
        mode: '0700'
      loop:
        - /var/lib/rancher/k3s/server/manifests
    - name: Apply K3s hardening on initial cluster node if enabled
      include_role:
        name: k3s_hardening
      vars:
        k3s_node_type: cluster-init
        role_server_flags: "{{ server_flags | default('') }}"

    - name: Install K3s on initial cluster node
      include_role:
        name: k3s_install
      vars:
        k3s_node_type: cluster-init
        k3s_api_host: "{{ kube_api_host }}"
        k3s_fqdn: "{{ fqdn | default(kube_api_host) }}"
        public_ip: "{{ ansible_host }}"
        k3s_node_external_ip: "{{ k3s_node_external_ip | default(true) }}"
        role_server_flags: "{{ server_flags | default('') }}"
        k3s_channel: "{{ channel | default('latest') }}"

    - name: Get node-token from master
      slurp:
        src: /var/lib/rancher/k3s/server/node-token
      register: node_token_output

    - name: Save node_token to local file
      copy:
        content: "{{ node_token_output.content | b64decode }}"
        dest: "{{ node_token_file }}"
      delegate_to: localhost
      run_once: true
      become: false

    - name: Read kubeconfig file contents
      slurp:
        src: /etc/rancher/k3s/k3s.yaml
      register: kubeconfig_contents

    - name: Save kubeconfig locally
      copy:
        content: "{{ kubeconfig_contents.content | b64decode }}"
        dest: "{{ kubeconfig_file }}"
      delegate_to: localhost
      run_once: true
      become: false

    # Pick first cp-having host for kubeconfig URL — split-role master may be etcd-only and have no apiserver.
    - name: Pick a kube-apiserver-bearing host for the kubeconfig server URL
      set_fact:
        kube_api_apiserver_host: >-
          {{ (groups['all'] | map('extract', hostvars)
              | selectattr('node_roles', 'defined')
              | selectattr('node_roles', 'contains', 'cp')
              | map(attribute='ansible_host') | list | first)
            | default(kube_api_host, true) }}
      delegate_to: localhost
      run_once: true
      become: false

    # Prefer a real fqdn for kubeconfig; otherwise use a cp-bearing host since split-role master may be etcd-only.
    - name: Compute kubeconfig server URL
      set_fact:
        kubeconfig_server_url: "https://{{ fqdn | default(kube_api_apiserver_host, true) }}:6443"
      delegate_to: localhost
      run_once: true
      become: false

    # Match any server: line — k3s_install role may have rewritten 127.0.0.1 to master IP before we slurped.
    - name: Update kubeconfig server address
      lineinfile:
        path: "{{ kubeconfig_file }}"
        regexp: '^\s*server:\s+https://[^\s]+'
        line: "    server: {{ kubeconfig_server_url }}"
      delegate_to: localhost
      run_once: true
      become: false

- hosts: all
  become: true
  gather_facts: false
  strategy: free
  vars_files: [vars.yaml]
  vars:
    node_token_file: "/tmp/node_token.txt"
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null"
  pre_tasks:
    - include_role:
        name: setup
    - name: Gather facts
      setup:

  tasks:
    - name: Read node_token from local file
      slurp:
        src: "{{ node_token_file }}"
      delegate_to: localhost
      run_once: true
      become: false
      register: node_token_file_content

    - name: Set node_token fact from file content
      set_fact:
        node_token: "{{ node_token_file_content.content | b64decode | trim }}"
      run_once: true
      delegate_to: localhost

    - name: Create K3s server manifests directory on server nodes
      file:
        path: "{{ item }}"
        state: directory
        mode: '0700'
      loop:
        - /var/lib/rancher/k3s/server/manifests
      when: inventory_hostname in groups.get('servers', [])

    - name: Apply K3s hardening on server nodes if enabled (before installation)
      include_role:
        name: k3s_hardening
      vars:
        k3s_node_type: server
        role_server_flags: "{{ server_flags | default('') }}"
        kubernetes_version: "{{ kubernetes_version }}"
      when:
        - inventory_hostname not in groups.get('master', [])
        - inventory_hostname in groups.get('servers', [])

    - name: Install K3s on server nodes
      include_role:
        name: k3s_install
      vars:
        k3s_node_type: server
        k3s_api_host: "{{ kube_api_host }}"
        k3s_fqdn: "{{ fqdn | default(kube_api_host) }}"
        k3s_token: "{{ node_token }}"
        public_ip: "{{ ansible_host }}"
        k3s_node_external_ip: "{{ k3s_node_external_ip | default(true) }}"
        role_server_flags: "{{ server_flags | default('') }}"
        k3s_channel: "{{ channel | default('latest') }}"
      when:
        - inventory_hostname not in groups.get('master', [])
        - inventory_hostname in groups.get('servers', [])

    - name: Apply K3s hardening on agent nodes if enabled
      include_role:
        name: k3s_hardening
      vars:
        k3s_node_type: agent
        role_server_flags: "{{ server_flags | default('') }}"
        role_worker_flags: "{{ worker_flags | default('') }}"
      when:
        - inventory_hostname in groups.get('workers', [])
        - inventory_hostname not in groups.get('master', [])

    - name: Validate required variables for agent installation
      assert:
        that:
          - kube_api_host | default('') | length > 0
          - node_token    | default('') | length > 0
        fail_msg: "Required variables missing: kube_api_host='{{ kube_api_host | default('UNDEFINED') }}', node_token='{{ node_token | default('UNDEFINED') }}'"
      when:
        - inventory_hostname in groups.get('workers', [])
        - inventory_hostname not in groups.get('master', [])

    - name: Install K3s on agent nodes
      include_role:
        name: k3s_install
      vars:
        k3s_node_type: agent
        k3s_api_host: "{{ kube_api_host }}"
        k3s_fqdn: "{{ fqdn | default(kube_api_host) }}"
        k3s_token: "{{ node_token }}"
        public_ip: "{{ ansible_host }}"
        k3s_node_external_ip: "{{ k3s_node_external_ip | default(true) }}"
        k3s_worker_flags: "{{ worker_flags | default('') }}"
        k3s_channel: "{{ channel | default('latest') }}"
      when:
        - inventory_hostname in groups.get('workers', [])
        - inventory_hostname not in groups.get('master', [])

- hosts: localhost
  connection: local
  gather_facts: false
  vars_files: [vars.yaml]
  tasks:
    - name: Wait until all K3s system pods are Running or Succeeded
      kubernetes.core.k8s_info:
        kubeconfig: "{{ kubeconfig_file }}"
        api_version: v1
        kind: Pod
        namespace: kube-system
      register: k3s_pods_check
      until: >
        k3s_pods_check.resources is defined and
        (k3s_pods_check.resources | rejectattr('status.phase', 'in', ['Running', 'Succeeded']) | list | length == 0)
      retries: 5
      delay: 60
